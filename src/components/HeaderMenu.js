import React from 'react'

export default function HeaderMenu() {
    return (
        <form>
            <label>Store</label>
            <select type="select"></select>
            <label>Genre</label>
            <select type="select"></select>
            <label>Price</label>
            <select type="select"></select>
        </form>
    )
}
